<% if @teachers.empty? %>
<p>Nenhum professor disponível</p>
<% end %>


<% @teachers.each do |teacher| %>
  <h1><%= link_to teacher.name, teacher %></h1>
  <h1><%= teacher.name %></h1>
  <p><%= teacher.bio %></p>
   <p><%= teacher.email %></p>
   <p><%= link_to 'Apagar', teacher_path(teacher), method: :delete %></p>
<% end %>

  <h2><%= link_to "Voltar", root_path %></h2>
  